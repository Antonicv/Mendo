/* gmap.js — DISABLED
   Replaced by js/leaflet-map.js (Leaflet + OpenStreetMap).
   Backup available at js/gmap.js.bak
*/

// Prevent accidental execution of legacy Google Maps code
(function(){
  if (typeof console !== 'undefined') console.info('gmap.js disabled — using leaflet-map.js instead.');
})();